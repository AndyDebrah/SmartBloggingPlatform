DB_URL=jdbc:mysql://localhost:3306/smart_blog_dev
DB_USERNAME=dev_user
DB_PASSWORD=change_me

# OAuth (optional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Add other environment-specific secrets here
## Example .env file for local development
# Copy this file to `.env` and fill with real secrets. Do NOT commit `.env`.

# Database connection used by Spring Boot (overrides application-local.properties defaults)
# Example URL format: jdbc:mysql://<host>:<port>/<database>?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
DB_URL=jdbc:mysql://localhost:3306/smart_blog?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
DB_USER=root
DB_PASS=aND#.814352

